---
// Superseded by src/lib/fragments.ts#renderSetsTableFragment, which both the
// full-page /sets route and the htmx partial /api/sets endpoint call so the
// markup only exists once. Kept out of the import graph; safe to delete.
---
