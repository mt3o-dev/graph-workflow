---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { t, resolveLocale } from "../../i18n";
import { getCurrentUser } from "../../lib/session";

const user = await getCurrentUser(Astro.cookies);
if (!user) return Astro.redirect(`/login?next=${Astro.url.pathname}`);

const locale = resolveLocale({
  queryLang: Astro.url.searchParams.get("lang"),
  acceptLanguage: Astro.request.headers.get("accept-language"),
});
const error = Astro.url.searchParams.get("error");
---

<BaseLayout title={t("live.join.title", locale)} locale={locale}>
  <div class="card" style="max-width:420px;margin-inline:auto">
    <h1>{t("live.join.title", locale)}</h1>
    {error === "notfound" && <p class="field-error">{t("live.join.notFound", locale)}</p>}
    <form method="get" action="/live/enter" class="stack">
      <div class="field">
        <label for="code">{t("live.join.codeLabel", locale)}</label>
        <input
          id="code"
          name="code"
          required
          maxlength="5"
          minlength="5"
          autocapitalize="characters"
          placeholder={t("live.join.codePlaceholder", locale)}
          style="text-transform:uppercase;letter-spacing:0.2em;font-size:1.5rem;text-align:center"
        />
      </div>
      <button type="submit" class="btn-primary">{t("live.join.submit", locale)}</button>
    </form>
  </div>
</BaseLayout>
