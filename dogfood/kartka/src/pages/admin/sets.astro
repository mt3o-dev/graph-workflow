---
import BaseLayout from "../../layouts/BaseLayout.astro";
import AdminNav from "../../components/AdminNav.astro";
import { t, resolveLocale } from "../../i18n";
import { requireAdminPage } from "../../lib/adminGuard";
import { getContainer } from "../../di/container";
import { listSetsForAdmin } from "../../core/usecases/adminUsecases";
import { parsePageQuery } from "../../lib/pageQuery";
import { renderAdminSetsTableFragment } from "../../lib/fragments";

const locale = resolveLocale({
  queryLang: Astro.url.searchParams.get("lang"),
  acceptLanguage: Astro.request.headers.get("accept-language"),
});

const gate = await requireAdminPage(Astro.cookies, locale, Astro.url.pathname);
if (gate instanceof Response) return gate;
const user = gate;

const { setRepo } = await getContainer();
const query = parsePageQuery(Astro.url.searchParams, { defaultSortBy: "createdAt" });
const data = await listSetsForAdmin(setRepo, user, query);
const tableHtml = renderAdminSetsTableFragment({ data, sortBy: query.sortBy, sortDir: query.sortDir, locale });
---

<BaseLayout title={t("admin.sets.title", locale)} locale={locale}>
  <h1>{t("admin.title", locale)}</h1>
  <AdminNav active="sets" locale={locale} />
  <h2>{t("admin.sets.title", locale)}</h2>
  <Fragment set:html={tableHtml} />
</BaseLayout>
