# dependencies (bun install)
node_modules

# output
out
dist
*.tgz
.astro

# app data (sqlite db + local uploads) and generated env types
/data/
src/env.gen.d.ts

# scratch/probe files agents leave behind (rm is policy-denied in this
# environment) — underscore-prefixed test files are scratch, never real
# suite members; see docs/DOGFOODING.md issue #30. Also cover root-level
# tmp_*/probe* scripts some review agents drop outside tests/.
tests/_*
/tmp_*
/probe*

# code coverage
coverage
*.lcov

# logs
logs
_.log
report.[0-9]_.[0-9]_.[0-9]_.[0-9]_.json

# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo

# IntelliJ based IDEs
.idea

# Finder (MacOS) folder config
.DS_Store
